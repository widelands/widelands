name: Run fix_formatting.py
on: push
jobs: 
  run_formatting: 
    name: Formatting your code
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@v2
    - name: Invoking fix_formatting.py
      run: |
        sudo apt-get install clang-format clang-format-6.0 libc6 libllvm6.0 libstdc++6 python
        ./utils/fix_formatting.py
        git status
