Import('*')
from Distribute import distadd

distadd(env, 'src/io/SConscript')
distadd(env, 'src/io/SConscript.dist')
distadd(env, 'src/io/*.h')
distadd(env, 'src/io/*.cc')

SConscript('filesystem/SConscript.dist')

