Import('*')
from Distribute import distadd

distadd(env, 'src/network/SConscript')
distadd(env, 'src/network/SConscript.dist')
distadd(env, 'src/network/*.h')
distadd(env, 'src/network/*.cc')

SConscript('game_server/SConscript.dist')

